import { createContext, useReducer } from 'react';

import { pickPhaseReducer, pickPhaseStateInit } from '../reducers/Reducers';

// const PickPhaseContext = createContext(pickPhaseStateInit);

// export const PickPhaseProvider = ({ children }) => {
//   const [state, dispatch] = useReducer(pickPhaseReducer, pickPhaseStateInit);

// };

// export default pickPhaseContext;
